\* Contrast config: SAME scope as ubcc_liveness.cfg but on the PRE-fix system
\* (RecallOrphanCleanup removed — see ubcc_liveness_nocleanup.tla).
\* Expected result: RecallProgress VIOLATED (lasso counterexample) — proving the
\* orphan wedge exists without the fix, and (by contrast with the PASS run) that
\* the fix eliminates it.
SPECIFICATION FairSpecNoCleanup
CONSTANT Nodes = {0, 1, 2}
CONSTANT MaxEpoch = 2
CONSTANT TombstoneWindow = 3
PROPERTY RecallProgress
