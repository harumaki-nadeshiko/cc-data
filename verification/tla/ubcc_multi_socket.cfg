\* D2: multi-socket coherence with cross-socket message routing.
\* Modules = 4 planes (e.g. 2 nodes x 2 sockets), Home on plane 0.
\* Expected: PASS — routing (in-flight/drop/reorder) never corrupts the
\* directory; home transitions identical for local vs remote requesters.
SPECIFICATION Spec
CONSTANT Modules = {0, 1, 2, 3}
CONSTANT Home = 0
CONSTANT MaxEpoch = 3
INVARIANT DirCanonical
INVARIANT SingleOwner
INVARIANT EpochBounded
INVARIANT GrantEpochSane
