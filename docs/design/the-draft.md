# UBCC 正式交付文档内部草稿与叙事台账

> 本文件仅用于内部写作、审阅和后续同步，不属于正式交付件。正式交付件不得出现本文件
> 名称、内部限制、运行命令、开发过程或证据哈希。

## 1. 发布定位

三份正式文档按“发布会”方式组织：先给客户能够直接理解和复述的结论，再给架构、
机制和必要数据；完整实验矩阵、开发过程和运行环境信息不进入正文。

统一主叙事：

1. UBCC 将跨节点目录和仲裁从节点内 HN-F 资源域中分离，形成独立、可扩展的全局一致性层。
2. ResidentDir 与 Backstore 形成分层元数据体系，在固定 SRAM 预算下扩展等效追踪容量。
3. EP-RNF、EP-SNF 与 UBAdapter 将 UBCC 接入节点内 CHI 域，支持远程读、所有权迁移和共享转写者路径。
4. 形式化验证与 52 项故障资格矩阵共同支撑安全性、活性和可恢复传输故障能力。
5. 三项性能指标全部达到冻结验收口径；指标 3 的胜场是冻结 HA-VI 可执行参考模型、2N1S、O3、单向完成语义和 L3 压力条件下的配对比较。

## 2. 术语约定

正式文档允许使用的核心术语限定如下：

| 术语 | 正式定义 |
|---|---|
| UBCC | 跨节点缓存一致性方案及其全局目录控制器；需区分时写“UBCC 方案”或“UBCC 控制器” |
| EP | 节点内 CHI 域与 UBCC 之间的端点扩展层 |
| EP-RNF | 代表跨节点一致性域参与节点内 snoop 的端点 |
| EP-SNF | 将节点内请求接入 UBCC 路径的服务端点 |
| UBAdapter | 节点内 CHI 端点与 UBIO 之间的消息适配组件 |
| UBIO | 承载 UBCC 控制器的运行模块 |
| ResidentDir | UBCC 的 SRAM 驻留目录 |
| Backstore | ResidentDir 冷元数据的后备存储 |
| HA-VI | 指标 3 使用的 VI 协议可执行参考模型 |
| Inner 域 | 节点内标准 CHI 一致性域 |
| Outer 域 | 由 UBCC 管理的跨节点一致性域 |
| 单向完成语义 | 根操作不以同步 ClearResp 作为完成条件的冻结语义 |
| 核心场景组 | TC228-TC230 等权聚合 |
| 代表场景组 | TC231-TC235 按冻结定义聚合 |
| 等效追踪容量 | ResidentDir 与已持久化 Backstore 元数据的去重覆盖量 |
| ubsim |  |
| ub |  |

正式正文不再使用 `OurCC`。历史结果文件名、脚本变量名和原始证据字段不改名，但不在
客户正文中展示。

## 3. 正式文档必须排除的内容

1. SHA-256、commit、diff hash、binary hash、dirty worktree、clean checkout 等开发证据标识。
2. Docker 命令、CPU 绑定、timeout、磁盘门槛、supervisor、日志路径和结果目录。
3. 调试 Marker，例如 `RECALL-DIAG`、`UPGRADE-DIAG`、`UBFAULT`、`TRACE-PERF`。
4. 第一次运行失败、磁盘门槛中止、crash、retry storm、barrier 卡住等试错过程。
5. 旧的指标 2 理论值 `810 ns -> 78 ns` 和修复前的弱 baseline。
6. “遗憾、仅、明显落后、严重不足、不能证明、不得扩大”等高频防御式句式。
7. 本地开发仓路径、gem5 子仓展开路径、测试脚本路径和源码行号。

## 4. 限制与不进入正式正文的材料

### 4.1 永久故障在线恢复

当前架构没有冗余 Home、dirty 数据副本和在线重配置。该能力不属于 Q1-Q5，也不作为
本次发布主张。正式文档只在交付范围表中写“永久 Home 故障后的在线服务恢复不在本次
交付范围”，不展开缺陷分析。

### 4.2 完整 ARMv8 axiomatic/herd7 证明

R07 已在可执行 O3 范围完成。完整 ARMv8 axiomatic/herd7 证明留作合同另行要求时的
扩展项，不在正式正文中作为弱点展开。

### 4.3 HA-VI 范围

指标 3 使用 HA-VI 可执行参考模型，不等同于客户物理芯片实测。正式文档在摘要和指标 3
结论各写一次范围限定，不在每节重复辩护。

### 4.4 确定性重复

五对配对运行用于模型复现和 AB/BA 顺序平衡，不报告 t-test、p-value 或置信区间。
该统计约定留在内部证据说明，正式正文只写“五对配对运行”。

### 4.5 指标 2 的 TC138

TC138 为 -13.333333%，按冻结聚合规则保留。正式文档不写“失败”或“退化”，而写：
“TC138 体现了 dirty handoff 场景下的机制权衡；其结果完整纳入等权平均，聚合指标仍为
64.759276%。”该材料承担“排除选择性过滤”的论证职责。

### 4.6 指标 3 的局部子项

TC232 hot-key read 略偏 HA-VI，write 明显偏 UBCC；TC234 queued-token store 子阶段不
作为主值。正式正文只解释冻结主值聚合边界，不罗列所有不参与计权的子阶段。完整子项
可放附录。

## 5. 叙事调整台账

| ID | 原材料 | 调整方式 | 正式叙事 |
|---|---|---|---|
| N01 | OurCC/CC-EP/UBCC 混用 | 术语收敛 | 对外统一称 UBCC；EP 只指端点层 |
| N02 | 从开发历程和修复开始 | 删除时间线 | 从方案定位、客户价值和最终结果开始 |
| N03 | “两个死锁及修复” | 机制化表达 | 写成请求去重、动态失效目标和 snoop 仲裁能力 |
| N04 | 指标 1 使用早期 microbench | 主张收缩 | 正文使用正式容量比和 guest 时延；microbench 不进入主结论 |
| N05 | 指标 2 使用旧理论值 | 实验重组 | 使用 72-run 正式矩阵和 64.759276% 聚合 |
| N06 | 指标 3 用抽象跳数作裁决 | 评价口径切换 | 使用 HA-VI 可执行参考模型的配对结果 |
| N07 | 每个子指标都要赢 | 重新定义故事 | 赢的是冻结核心场景组和代表场景组，不是所有内部子阶段 |
| N08 | fault 首轮磁盘中止说明 | 删除 | 正文只报告最终 52/52 结果 |
| N09 | focused 模型边界长篇自审 | 精简 | 用“形式化模型 + 可执行验证”分层覆盖表表达 |
| N10 | 接口章节按函数和 Marker 编排 | 重构 | 按输入、输出、完成条件和重试语义编排 |
| N11 | MetaRNF 作为稳定公开模块 | 术语替换 | 对外使用 Backstore；MetaRNF 仅留内部实现说明 |
| N12 | 本地源码路径和行号 | 远端适配 | 正式文档仅保留模块级接口与集成边界 |
| N13 | SHA/dirty/image 进入正文 | 全部删除 | 只保留内部 evidence manifest |
| N14 | 文档同步机制写入客户正文 | 删除 | 属内部发布流程，不进入正式交付件 |
| N15 | 将 TC131、TC135-TC140、TC217 写成指标 1/2 的全部实验 | 结果分层 | 区分合同计分集与 TC120-TC147 支撑结果集 |

### 5.1 指标 1/2 结果分层说明

正式合同总分保持冻结口径：指标 1 由 TC131 计分，指标 2 由 TC135-TC140 和 TC217
计分。TC120-TC129、TC130-TC134、TC141、TC142-TC147 作为正式支撑结果进入正文，
但不在缺少冻结权重的情况下混入合同总分。

该调整不是删除实验，而是明确每组实验的论证职责：

- TC120-TC124：访问模式和低/高复用条件；
- TC125-TC129：Backstore 生命周期正确性；
- TC130-TC134：真实容量压力和多拓扑；
- TC141：shared-writer recovery 资格门禁；
- TC142-TC147：16N1S 代表应用价值。

TC142-TC147 使用外部冻结 16N1S 一轮正式矩阵，三个 profile 共 18/18 PASS。由于当前
只有一轮，不报告跨轮 CV；正式正文报告端到端均值和相对降幅，不把它们定义为新的合同
总分。

## 6. 正式集成边界

正式文档只写模块职责、输入输出、完成条件和重试语义，不展示源代码目录、构建命令或
本地证据位置。parallel_test_v2 仅保留必要字段的未验证填写模板，未知细节不推测。

## 8. 图形策略

计划生成以下主图：

1. `ubcc-system-architecture.drawio/png`：交付件 1 总体架构。
2. `ubcc-protocol-paths.drawio/png`：远程读、所有权迁移、共享转写者三条路径。
3. `ubcc-verification-stack.drawio/png`：交付件 2 验证分层。
4. `ubcc-two-phase-commit.drawio/png`：两阶段提交。
5. Metric1/Metric2 结果图：复用现有性能图并分别给出冻结口径 caption。
6. `ubcc-ha-vi-comparison.png`：Metric3 四组聚合柱状图。
7. Q1-Q5 资格结果图：正文保留图引用与 caption，图形制作不属于本轮文档修改。

当前标准 Docker 镜像没有 draw.io CLI。`.drawio` 文件作为可编辑源，PNG 由相同布局参数
生成；视觉校验包括尺寸、边界、文字溢出、透明度、色彩和图例。若甲方要求“必须由
draw.io 导出”，需在有 draw.io Desktop/CLI 的环境再执行一次同版式导出。

## 9. 后续同步规则

每次修改任一正式交付件时必须同步：

1. 更新本文件的叙事台账或审阅状态。
2. 更新对应 Markdown。
3. 更新相关 `.drawio` 与 PNG。
4. 生成对应 DOCX。
5. 检查正式文档无 `OurCC`、无 SHA、无 draft 名称、无运行命令和调试 Marker。

## 10. 第一版完成状态

| 项目 | 状态 |
|---|---|
| 三份正式 Markdown 重写 | 完成 |
| OurCC 术语清理 | 正式三册中为零 |
| SHA、dirty、运行命令和调试 Marker 清理 | 完成 |
| 模块级集成边界适配 | 完成 |
| 多级编号、段落缩进、A4、页码和附录 | 完成 |
| 六张主图的 draw.io、SVG、PNG | 完成 |
| DOCX 图片嵌入和 PDF 渲染 | 完成 |
| 图形结构化 QA | 完成 |
| parallel_test_v2 参数补齐 | 延后 |
| 脱敏标签替换 | 延后 |
| 品牌视觉人工复核 | 延后 |
| 封面审批信息 | 延后 |

## 11. Round 1 审阅记录

### 11.1 审阅者

- 视觉审阅 A：hitomi-agent。当前运行后端未提供可用图像理解，未输出逐页视觉结论；
- 视觉审阅 B：futsu-agent。当前配置的视觉模型不可用，任务未启动；
- 目标验收审阅：xhigh-intelligent-agent；
- 高管可读性审阅：high-intelligent-agent；
- 结构化视觉检查：DOCX OOXML、PDF 页数、字体嵌入、Draw.io 图源、宽高比、最小字号和图形资产清单。

### 11.2 已接受发现

1. 正式文档遵循发布事件原则，只聚合已满足冻结完成条件的事件。
2. Metric1 改为容量 spill/naive（spill 为 512K 容量约束角色）、时延 spill-512K Outer mean 减 spill-IdealDir
   Outer mean；结果为 10.534578 ns、21.069156 cycles。Round 1 暂按统一三角色合同描述，
   复审后确认当前证据核算需要在 Round 2 拆分说明。
3. 删除正式文档中的源路径、构建命令、证据哈希和工作区状态描述。
4. NetworkSim 不作为项目架构或组件，仅允许一次验证环境范围说明。
5. 增加 Phase-1 方案沿革、MESI/MOESI/MSI/VI 比较，并明确 Backend + Bloom 已被当前
   ResidentDir + Backstore 方案取代。
6. 增加 HA 理论/参考比较、可靠性故障模型和 16N Level-A 与端口级 Switch 边界披露。
7. 性能摘要改为简洁文字与表格，图移至对应 Metric 章节并补 caption。
8. Word 样式压缩正文和标题间距，表格改为左对齐 9 pt，图片高度受控；三册由 20/18/24 页
   收敛至 12/12/15 页。
9. 架构与流程图改为 Draw.io CLI 实际导出；新增总体架构、gem5 EP 架构和四张数据图。

### 11.3 复审后进入 Round 2 的问题

1. Metric1 当前保留证据由旧 profile 矩阵和独立 spill/IdealDir 矩阵组成，不能写成已经完成
   一个原生的 3×3 九点同矩阵。Round 2 将区分“当前证据核算”和“后续统一裸启动合同”。
2. 当前 final acceptance manifest 和旧 compact report 仍保存废弃 guest 时延口径。正式文档
   不引用旧值；内部台账保留该漂移，最终冻结时再统一重生成，不在发布正文展示哈希。
3. HA 比较需要增加一张假设与公平性表，并明确聚合优势不表示每个内部子项都占优。
4. 形式化模型清单需要区分当前实现、bounded/focused proof 和范围外能力，避免所有 PASS
   处于同一视觉层级。
5. 三份文档需要统一一张简洁的“结论与范围”表，但不得重新引入开头的大型指标摘要图。

### 11.4 延后发现

- 新的 Switch 微体系结构实验；
- 发布物哈希；
- ubsim 与 ub 的定义；
- parallel_test_v2 的实际参数、行为和输出细节。

其中 ubsim/ub 空白和 parallel_test_v2 模板是用户明确要求，不因评审器希望补全而改写。
像素级双视觉审阅因当前 agent 后端不可用而延后；本轮不将结构化 QA 冒充人工视觉结论。

### 11.5 修改摘要

Round 1 更新三份正式文档和本叙事台账：收敛术语与粗体使用，修正 Metric1 口径与运行核算，
补充设计沿革、HA/可靠性章节、图引用和范围披露，并将未验证信息明确保留为模板或延后项。

## 12. Round 2 审阅记录

### 12.1 审阅者与审阅目标

- 视觉审阅 A：hitomi-agent，后端不支持图像读取，只确认自动 QA 结果，未给出像素级判断；
- 视觉审阅 B：futsu-agent，配置模型仍不可用；
- 目标验收复审：xhigh-intelligent-agent；
- 高管可读性复审：high-intelligent-agent；
- 证据核算复审：核对 Metric1 当前保留证据、物理运行职责与未来统一采集合同；
- HA 公平性复审：核对客户确认/冻结参考假设、未建模硬件和聚合解释边界；
- 形式化证据分层复审：核对实现执行、bounded/focused 模型、理论分析和范围外能力；
- 三册一致性复审：核对“结论与范围”、NetworkSim、术语空白和模板状态。

### 12.2 已接受发现

1. Metric1 当前证据不得描述为已存在的三角色 3×3 九运行矩阵。容量证据来自 legacy
   72-run 指标 1/2 profile 矩阵中的三次 naive 与三次 spill-noopt；optimized 是支持证据，
   不进入容量分母或分子。
2. 修正时延证据来自独立的三次 spill-512K 与三次 spill-IdealDir，共 6 个物理 arms。当前
   1.515×、10.534578 ns 和 21.069156 cycles 由两个合格证据集按各自职责组合，不跨集合
   重复加权。
3. 后续标准化采集合同可以采用统一三角色 3×3 裸启动矩阵，但必须明确这是未来合同，不是
   当前证据事实。
4. 确定性重复仿真只支持可复现性，不作为统计置信度声明。
5. HA 比较增加假设与公平性边界表，区分客户确认/冻结的 2N1S、O3、完成语义、压力点、
   workload 和参考参数，与未建模的客户物理微体系结构、Switch 细节和 NetworkSim。
6. HA 聚合优势只表示冻结场景组主值的平均优势，不表示每个 testcase、内部阶段或子操作
   均占优。
7. 形式化验证增加证据层级表，分别标示当前实现 + 端到端执行、有界/focused 模型、理论
   分析和本次范围外能力，避免不同证据强度被放在同一层级解释。
8. 三份正式文档开头增加一致、简洁的“结论与范围”表，不新增大型摘要图，也不引入虚假
   OPEN 状态。
9. 增加 PDF 自动布局 QA，检查稀疏页、孤立标题、图片占比、越界、表格拉伸、页尺寸和最小
   字号；修复图片段落继承固定行距导致的 LibreOffice 负坐标越界。当前三册 13/14/17 页
   全部通过自动布局检查。
10. 图表 inventory 增加数据来源、公式、数值和文档引用，Metric1 图明确使用容量和
    spill/IdealDir 两套独立证据源，禁止读取废弃 guest-latency 字段。

### 12.3 延后或保持不变的事项

- ubsim 与 ub 的正式定义继续留空，等待用户提供；
- parallel_test_v2 继续保持仅供填写的未验证模板，不补写推测内容；
- 发布物哈希、源码位置、运行命令和内部证据路径继续不进入正式文档；
- NetworkSim 继续明确为项目范围外，不提出新实验；
- 端口级 Switch 微体系结构、客户物理 HA 和永久 Home 故障在线恢复保持范围外；
- 不新增实验请求，不修改脚本、图形或其他交付文件。

复审器提出补写 ubsim/ub 定义、补齐 parallel_test_v2、把未来 3×3 变成新的验收前置条件，
均因与用户明确要求或冻结原则冲突而不接受。高管复审中“指标 1/2 仍未闭环”“当前无 PDF/
正式图形”等判断与 Round 2 当前文件和证据状态不符，记为审阅上下文偏移，不据此修改正文。

### 12.4 Round 2 修改摘要

Round 2 的内容修订集中于三份正式 Markdown 与本草稿；其后的机械发布同步重新生成了 DOCX、
PDF、文档配对清单、图表数据血缘和自动 QA 报告。正式文档已纠正 Metric1 当前证据核算与
未来 3×3 合同的关系，保留冻结接受值；补充确定性重复的证据边界、HA 假设与公平性、形式化
证据层级，以及三册一致的“结论与范围”表。用户要求保留为空或仅作模板的内容未被补写，
NetworkSim 和未建模硬件继续保持范围外。

## 13. Round 3 审阅记录

### 13.1 审阅者

- 视觉审阅 A：hitomi-agent，后端仍不支持实际 PNG/PDF 图像理解；
- 视觉审阅 B：futsu-agent，配置模型仍不可用；
- 目标验收复审：xhigh-intelligent-agent；
- 高管可读性复审：high-intelligent-agent；
- 自动检查：203 项脚本测试、图形结构与血缘 QA、逐页 PDF 布局 QA、字体嵌入和文档同步。

### 13.2 已接受发现

1. EP-RNF snoop 仲裁和 TC224 waiter 退役 focused 模型只运行不变量，不应标为独立
   Liveness PASS；正式汇总改为 Safety PASS，Liveness 由可执行验证补充。
2. 16N 摘要和应用结论统一写为 16N1S Level-A 协议节点规模与端点能力，不将其简称为
   端口级 Switch 验证。
3. 未来统一三角色 3×3 只保留为验收后可选复现方案，明确不是本轮 Gate，未经合同变更
   不据此要求新增实验。
4. Metric1 图表血缘增加两套独立证据集的角色、重复数、运行数和聚合规则，并明确跨集合
   不重复计权。
5. 图形与布局 QA 顶层声明为自动结构检查，人工像素级视觉审阅未执行，避免把自动 PASS
   误称为人工审美验收。

### 13.3 未接受或延后

- 不删除正文中的 ubsim 名称；用户明确要求带入该名称，同时解释和术语表保持空白；
- 不补写 parallel_test_v2 的未知行为；
- 不新增三指标大图，保持性能文档开头以文字和表格表达；
- 不新增 Switch、HA、Metric1 或其他实验；
- 不把发布哈希、内部证据路径或命令加入正式正文；
- 人工双视觉审阅继续因当前后端能力限制而延后。

### 13.4 修改摘要

Round 3 仅做严谨性和发布记录收口：纠正 focused 模型证据等级、统一 16N Level-A 边界、
明确 3×3 非验收 Gate、增强 Metric1 图表血缘，并为自动 QA 加入方法与限制声明。未扩大
技术主张，未增加实验或项目代码修改。

### 13.5 最终复审结论

目标验收复审给出有条件通过，剩余真实问题均已在 Round 3 修正：focused Liveness 证据等级、
16N Level-A 可摘录表述、3×3 非 Gate 属性和自动 QA 限制。其余“旧 UNPROVEN 一页仍被
正式三册引用”“当前正式三册存在 71/71 与 146/146 冲突”“Round/figure/layout 文件不存在”
经精确搜索均不成立，判定为审阅上下文偏移。

本轮后不启用 Round 4：没有剩余 P0/P1 技术内容问题，继续修改只会增加冻结期漂移。仍然
延后的项目是用户明确保留的 ubsim/ub 空白、parallel_test_v2 填写信息、品牌与审批信息，
以及由于 agent 后端不可用而未执行的人工像素级双视觉审阅。

## 14. Visual Round 4 审阅记录

### 14.1 审阅者与范围

- hitomi-agent：逐页检查三册 44 页 PNG，并检查 9 张独立图形；
- futsu-agent：对相同页面和图形进行独立视觉审阅；
- 本轮只接受字体、间距、分页、表格、图注、图形重叠和视觉密度意见，不评审技术内容。

### 14.2 已接受发现

1. 表格中的 `InvalidateReq`、`UpgradeAckNotify`、`InvalidateAck`、`frontier`、`evict` 等
   英文标识符存在字符中间硬拆，改为整词不可断行。
2. 三册术语表跨页形成稀疏尾页，改用独立紧凑表格样式；ubsim/ub 定义单元格仍保持空白。
3. 协议总览第 4、6 页的长图注说明被视觉上识别为悬空续句，将解释文字移到图前正文，
   图下注仅保留单行短标题；其他数据图和验证图采用同一规则。
4. 正文出现孤立单字/短尾行，增加 widow/orphan、中文行首行尾控制和短尾保护。
5. 总体架构、gem5 EP、协议路径、验证链和两阶段提交图存在标签、连线与框体碰撞，重新
   分配节点间距和边标签偏移，并提高标题和边标签字号。
6. 三册稀疏尾页消除，页数由 13/14/17 收敛为 12/13/17。

### 14.3 未接受或延后

- 不补齐 ubsim/ub 空白定义；
- 不增加封面项目编号、日期或品牌元素，等待用户提供；
- 不为手工目录虚构页码或点引导线；
- 不改变任何指标数值、技术结论或范围声明；
- 数据图配色统一、图面留白等仅作可选润色，留待下一轮复审确认是否仍有必要。

### 14.4 修改摘要

Visual Round 4 修复五张 Draw.io 图的主要碰撞与小字号问题；Word 生成器增加英文整词换行、
紧凑术语表、widow/CJK 行控制和短尾保护；正式 Markdown 只移动图示解释并缩短图注。
全部修改均为视觉与排版调整。

## 15. Visual Round 5 审阅记录

### 15.1 审阅者与范围

- hitomi-agent：复查 Visual Round 4 后的三册 42 页和 9 张图；
- futsu-agent：对相同产物独立复查；
- 本轮继续只处理视觉问题，不改变技术内容。

### 15.2 已接受发现

1. Deliverable 3 术语表仍产生稀疏末页；DOCX 输出将两列表转换为
   `术语｜说明｜术语｜说明` 四列配对布局，减少表高并保持 ubsim/ub 定义空白。
2. U+FEFF 在 LibreOffice 中不能可靠阻止英文标识符断行，改用 U+2060；对
   `InvalidateReq`、`UpgradeAckNotify`、`InvalidateAck`、`frontier`、`evict` 所在窄单元格
   额外使用 8 pt 定向字号，PDF 文本复核确认不再字符中断行。
3. Metric1 数据图移除与左右子图标题竞争的冗余总标题，保留两个子图标题和文档图注。
4. UBCC 总体架构图进一步移除中央冗余边标签并增加 Node0 Outer domain 横向留白，消除
   `Outer messages`、`message conversion`、`hot metadata` 和 `generic packets` 的碰撞。

### 15.3 未接受或延后

- 不修改 ubsim/ub 空白定义；
- 不改变目录页、封面或正文技术内容；
- gem5 图、two-phase 图的轻微标签紧凑仅作为可选项，由最后一轮复审决定是否需要继续调整；
- 数据图颜色语义统一属于可选润色，当前区分度和可读性已满足发布要求。

### 15.4 修改摘要

Visual Round 5 重点关闭剩余硬伤：合并稀疏术语尾页、阻止英文标识符硬拆、消除 Metric1
标题叠压和总体架构图中央碰撞。所有修改均限定在 Word 布局与图形构图。

## 16. Visual Round 6 审阅记录

### 16.1 审阅者与分歧处理

- hitomi-agent：报告部分表格仍有英文标识符硬拆和 TC229 单字孤行；
- futsu-agent：判定没有剩余 must-fix，仅列出少量可选图形润色；
- 对分歧项使用当前 PDF 的逐页 `pdftotext -layout` 结果裁决，确认 hitomi 报告的
  `InvalidateReq`、`UpgradeAckNotify`、`InvalidateAck`、`RecallResp`、`sliding`、`feature`
  确实发生字符中断，TC229 也存在单字“写”孤行。

### 16.2 已接受发现与修改

1. 在表格段落加入 Word `wordWrap=0`，禁止 Latin 单词在字符中间断行；U+2060 和定向
   8 pt 继续作为双保险。
2. TC229 句子仅重排同义语序，避免引号内短语产生孤立单字，不改变技术含义。
3. 稀疏术语尾页、总体架构碰撞和 Metric1 标题叠压已由两位 reviewer 确认关闭，不再修改。

### 16.3 可选项不再修改

- gem5 图个别标签紧凑、two-phase timeout 靠近连线、部分数据图轴标签偏小均属于可接受
  范围，不继续扩大图形改动；
- 封面、目录和空白术语定义保持用户要求；
- 本轮后以 PDF 文本敏感词跨行检查、自动布局 QA 和字体嵌入结果作为收口门槛。
